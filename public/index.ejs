<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Artnguide 포인트 이전 안내 및 동의</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
        }
        input[type="text"],
        input[type="email"] {
            width: 100%;
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <h2>Artnguide 포인트 이전 안내 및 동의</h2>
    <form id="agreeForm" action="/submit" method="POST">
        <div class="form-group">
            <label for="email">이메일:</label>
            <input type="email" name="email" id="email" required />
        </div>
        <div class="form-group">
            <label for="agree">동의 여부:</label>
            <input type="checkbox" id="agree" name="agree" required />
        </div>
        <div class="form-group">
            <label for="transformId">포인트 이전 아이디:</label>
            <input type="text" id="transformId" name="transformId" required />
        </div>
        <button type="submit">동의하기</button>
    </form>
</body>
<script>
    // URL에서 이메일 파라미터 가져오기
    function getEmailFromURL() {
        const urlParams = new URLSearchParams(window.location.search);
        return urlParams.get('email') || '';
    }

    // 페이지 로드 시 이메일 설정
    window.onload = function() {
        const email = getEmailFromURL();
        document.getElementById('email').value = email;
    }
</script>
</html>
